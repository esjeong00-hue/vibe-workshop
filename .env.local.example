# Supabase 연동에 필요한 값입니다.
# 이 파일을 복사해 `.env.local` 로 만든 뒤, 아래 두 값을 본인 프로젝트 값으로 채워주세요.
# Supabase 대시보드 > Project Settings > Data API (또는 API) 에서 확인할 수 있습니다.
# .env.local 은 git 에 커밋되지 않습니다.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
